<!-- Git commands -->

<!--
* git --version
* git add filename  // git add . -> To add all files.  -> move the files to staging
* git commit -m 'commit message' -> To commit the changes to the branch.
* git status
* git log
* git branch  -> Check the current branch
* git checkout branchName  -> Moved to other branch
* git merge branchName  -> Merge the other branch to current branch
* git reset --hard commit-log-code -> remove the latest commit and move back to specified commit
* git reset --soft commit-log-code -> keep all the commit and move to specified commit

 -->
